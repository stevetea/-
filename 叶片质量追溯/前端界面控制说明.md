# 前端用户界面控制说明

## 📋 概述

本文档说明前端用户界面的控制位置和结构，帮助您快速找到需要修改的文件。

## 🗂️ 项目结构

```
yepian/叶片质量追溯/
├── pages/              # 页面组件（主要界面文件）
├── pages.json          # 页面路由和全局样式配置
├── App.vue              # 应用根组件（全局样式和逻辑）
├── main.js             # 应用入口文件
├── manifest.json       # 应用配置文件
├── index.html          # H5版本入口HTML
├── static/             # 静态资源（图片、图标等）
└── utils/              # 工具函数（请求、存储等）
```

## 🎨 界面控制位置

### 1. **全局样式控制**

#### `pages.json` - 全局导航栏样式
```json
"globalStyle": {
  "navigationBarTextStyle": "black",           // 导航栏文字颜色
  "navigationBarTitleText": "叶片质量追溯系统", // 导航栏标题
  "navigationBarBackgroundColor": "#667eea",   // 导航栏背景色 ⭐
  "backgroundColor": "#F8F8F8"                 // 页面背景色 ⭐
}
```
**位置**：`yepian/叶片质量追溯/pages.json` 第75-80行

#### `App.vue` - 全局页面样式
```css
page {
  background-color: #f5f5f5;  // 全局页面背景色 ⭐
}
```
**位置**：`yepian/叶片质量追溯/App.vue` 第30-32行

### 2. **页面组件控制**

每个页面的界面都在对应的 `.vue` 文件中控制：

| 页面 | 文件路径 | 说明 |
|------|---------|------|
| **登录页** | `pages/login/login.vue` | 登录界面样式和逻辑 |
| **首页** | `pages/index/index.vue` | 首页界面 |
| **扫码追溯** | `pages/scan/scan.vue` | 扫码页面 |
| **追溯详情** | `pages/trace-detail/trace-detail.vue` | 详情页 |
| **质检报告** | `pages/qc-input/qc-input.vue` | 质检录入页 |
| **叶片列表** | `pages/blade-list/blade-list.vue` | 列表页 |
| **工序录入** | `pages/process-input/process-input.vue` | 工序录入页 |
| **创建叶片** | `pages/blade-create/blade-create.vue` | 创建叶片页 |
| **人员管理** | `pages/user-manage/user-manage.vue` | 人员管理页 |
| **数据统计** | `pages/statistics/statistics.vue` | 统计页 |
| **YOLO检测** | `pages/yolo-detect/yolo-detect.vue` | 检测页 |

### 3. **页面样式结构**

每个 `.vue` 文件包含三个部分：

```vue
<template>
  <!-- HTML结构 -->
</template>

<script>
  // JavaScript逻辑
</script>

<style scoped>
  /* CSS样式 ⭐ 界面样式在这里控制 */
</style>
```

## 🎯 常用修改场景

### 场景1：修改全局导航栏颜色

**文件**：`pages.json`
```json
"globalStyle": {
  "navigationBarBackgroundColor": "#你的颜色",  // 修改这里
}
```

### 场景2：修改页面背景色

**文件**：`App.vue` 或具体页面的 `.vue` 文件
```css
page {
  background-color: #你的颜色;
}
```

### 场景3：修改登录页面样式

**文件**：`pages/login/login.vue`
- 背景色：`.login-container` 样式中的 `background`
- 输入框：`.input` 样式
- 按钮：`.login-btn` 样式
- 表单卡片：`.login-form` 样式

### 场景4：修改首页样式

**文件**：`pages/index/index.vue`
- 在 `<style scoped>` 部分修改样式

### 场景5：修改列表页样式

**文件**：`pages/blade-list/blade-list.vue`
- 列表项样式
- 卡片样式
- 状态标签样式

## 📝 样式修改示例

### 示例1：修改登录页面背景

**文件**：`pages/login/login.vue`
```css
.login-container {
  background: linear-gradient(135deg, #FFE4E1 0%, #FFB6C1 100%);
  /* 修改上面的颜色值即可 */
}
```

### 示例2：修改输入框样式

**文件**：`pages/login/login.vue`
```css
.input {
  background: #f8f9fa;      /* 输入框背景色 */
  border: none;             /* 边框 */
  border-radius: 12rpx;     /* 圆角 */
  /* 修改这些属性即可 */
}
```

### 示例3：修改按钮样式

**文件**：`pages/login/login.vue`
```css
.login-btn {
  background: linear-gradient(135deg, #FFB6C1 0%, #FFB6C1 100%);
  color: #ffffff;
  border-radius: 12rpx;
  /* 修改这些属性即可 */
}
```

## 🔍 快速定位样式

### 方法1：通过页面名称查找
1. 确定要修改的页面（如：登录页）
2. 找到对应的 `.vue` 文件（`pages/login/login.vue`）
3. 在 `<style scoped>` 部分查找和修改样式

### 方法2：通过类名查找
1. 在浏览器/开发者工具中查看元素的类名
2. 在对应的 `.vue` 文件中搜索该类名
3. 修改对应的样式

### 方法3：通过全局搜索
使用 IDE 的全局搜索功能：
- 搜索颜色值（如：`#667eea`）
- 搜索类名（如：`login-container`）

## 🎨 颜色配置位置汇总

| 位置 | 文件 | 说明 |
|------|------|------|
| 全局导航栏背景 | `pages.json` | `globalStyle.navigationBarBackgroundColor` |
| 全局页面背景 | `App.vue` | `page { background-color }` |
| 登录页背景 | `pages/login/login.vue` | `.login-container { background }` |
| 登录表单背景 | `pages/login/login.vue` | `.login-form { background }` |
| 按钮背景 | 各页面 `.vue` | `.login-btn` 或其他按钮类 |
| 输入框背景 | 各页面 `.vue` | `.input` 类 |

## 📱 不同平台的样式

### 微信小程序
- 样式在 `.vue` 文件的 `<style>` 中
- 使用 `rpx` 单位（响应式像素）

### H5版本
- 样式同样在 `.vue` 文件中
- 可以使用 `px` 或 `rpx` 单位

### App版本
- 样式在 `.vue` 文件中
- 建议使用 `rpx` 单位保持一致性

## 🔧 工具函数位置

界面相关的工具函数在 `utils/` 目录：

- `utils/request.js` - API请求封装
- `utils/storage.js` - 本地存储
- `utils/config.js` - 配置文件（状态映射等）

## ⚠️ 注意事项

1. **样式作用域**：
   - `<style scoped>` - 只影响当前组件
   - `<style>` - 全局样式（在 `App.vue` 中）

2. **单位使用**：
   - 推荐使用 `rpx`（响应式像素）
   - `1rpx = 屏幕宽度 / 750`

3. **颜色格式**：
   - 十六进制：`#FFB6C1`
   - RGB：`rgb(255, 182, 193)`
   - RGBA：`rgba(255, 182, 193, 0.8)`

4. **修改后需要**：
   - 保存文件
   - 重新编译项目
   - 清除缓存（如需要）

## 📚 相关文件

- **页面路由配置**：`pages.json`
- **应用配置**：`manifest.json`
- **全局样式**：`App.vue`
- **入口文件**：`main.js`
- **静态资源**：`static/` 目录

## 🎯 快速修改指南

1. **确定要修改的页面** → 找到对应的 `.vue` 文件
2. **打开文件** → 找到 `<style scoped>` 部分
3. **定位样式类** → 使用类名或搜索功能
4. **修改样式** → 更改颜色、尺寸等属性
5. **保存并测试** → 重新编译查看效果

## 💡 提示

- 使用浏览器的开发者工具可以快速定位样式
- 修改前建议备份原文件
- 使用版本控制（Git）管理修改历史
- 保持样式命名的一致性

