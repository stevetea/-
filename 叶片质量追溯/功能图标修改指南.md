# 功能图标修改指南

## 📋 当前图标实现方式

首页功能卡片使用的是 **Emoji 表情符号**作为图标，显示在圆形渐变背景中。

## 🎨 图标修改方法

### 方法1：更换 Emoji（最简单）

**位置**：`pages/index/index.vue`

直接替换 `<text class="icon">` 中的 emoji：

```vue
<view class="icon-wrapper">
  <text class="icon">📷</text>  <!-- 替换这里的emoji -->
</view>
```

**常用 Emoji 图标：**
- 📷 相机
- ✏️ 编辑/录入
- 🔬 检测/实验
- ✅ 质检/完成
- 📋 列表
- ➕ 添加/创建
- 👥 人员/用户
- 📊 统计/图表
- 🤖 机器人/AI
- 🔍 搜索
- ⚙️ 设置
- 📱 设备
- 🏭 工厂
- 📦 产品
- 🔔 通知

### 方法2：使用图片图标

**步骤：**

1. **准备图标图片**
   - 将图标图片放到 `static/` 目录
   - 建议尺寸：100x100 或 200x200 像素
   - 格式：PNG（支持透明背景）

2. **修改代码**

将 emoji 替换为 `<image>` 标签：

```vue
<!-- 原来的方式 -->
<view class="icon-wrapper">
  <text class="icon">📷</text>
</view>

<!-- 改为图片 -->
<view class="icon-wrapper">
  <image class="icon-image" src="/static/icons/camera.png" mode="aspectFit"></image>
</view>
```

3. **添加样式**

在 `<style>` 中添加：

```css
.icon-image {
  width: 50rpx;
  height: 50rpx;
}
```

### 方法3：使用图标字体库（推荐）

**使用 iconfont 图标库：**

1. **引入图标字体**

在 `App.vue` 的 `<style>` 中引入：

```css
@import url('//at.alicdn.com/t/font_xxxxx.css');
```

2. **修改代码**

```vue
<view class="icon-wrapper">
  <text class="icon iconfont icon-camera"></text>
</view>
```

3. **调整样式**

```css
.function-item .icon {
  font-size: 50rpx;
  font-family: 'iconfont';  /* 添加字体 */
}
```

## 🎨 修改图标背景颜色

图标背景在 `.icon-wrapper` 中控制，不同功能类型有不同的背景色：

**位置**：`pages/index/index.vue` 第439-453行

```css
/* primary类型 - 扫码追溯 */
.function-item.primary .icon-wrapper {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

/* success类型 - 数据统计 */
.function-item.success .icon-wrapper {
  background: linear-gradient(135deg, #4caf50 0%, #45a049 100%);
}

/* warning类型 - 创建叶片 */
.function-item.warning .icon-wrapper {
  background: linear-gradient(135deg, #ff9800 0%, #f57c00 100%);
}

/* info类型 - 人员管理 */
.function-item.info .icon-wrapper {
  background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%);
}
```

**改为淡粉色：**

```css
.function-item.primary .icon-wrapper {
  background: linear-gradient(135deg, #FFB6C1 0%, #FFB6C1 100%);
}

.function-item.success .icon-wrapper {
  background: linear-gradient(135deg, #FFB6C1 0%, #FFB6C1 100%);
}

.function-item.warning .icon-wrapper {
  background: linear-gradient(135deg, #FFB6C1 0%, #FFB6C1 100%);
}

.function-item.info .icon-wrapper {
  background: linear-gradient(135deg, #FFB6C1 0%, #FFB6C1 100%);
}
```

## 📝 具体修改示例

### 示例1：更换扫码追溯图标

**文件**：`pages/index/index.vue` 第20行

```vue
<!-- 原代码 -->
<text class="icon">📷</text>

<!-- 改为其他emoji -->
<text class="icon">🔍</text>  <!-- 搜索图标 -->
<text class="icon">📱</text>  <!-- 手机图标 -->
<text class="icon">📸</text>  <!-- 拍照图标 -->
```

### 示例2：使用图片图标

```vue
<view class="icon-wrapper">
  <image class="icon-image" src="/static/icons/scan.png" mode="aspectFit"></image>
</view>
```

**添加样式：**

```css
.icon-image {
  width: 50rpx;
  height: 50rpx;
}
```

### 示例3：统一所有图标背景为淡粉色

**文件**：`pages/index/index.vue` 第439-453行

```css
.function-item.primary .icon-wrapper,
.function-item.success .icon-wrapper,
.function-item.warning .icon-wrapper,
.function-item.info .icon-wrapper {
  background: linear-gradient(135deg, #FFB6C1 0%, #FFB6C1 100%);
}
```

## 🎯 图标位置汇总

所有功能图标都在 `pages/index/index.vue` 中：

| 功能 | 行号 | 当前图标 | 类型 |
|------|------|---------|------|
| 扫码追溯 | 20, 41, 76 | 📷 | primary |
| 工序录入 | 27 | ✏️ | success |
| 荧光检测 | 48 | 🔬 | success |
| 质检录入 | 55 | ✅ | warning |
| 待检列表 | 62 | 📋 | info |
| 创建叶片 | 83 | ➕ | warning |
| 人员管理 | 90 | 👥 | info |
| 数据统计 | 97 | 📊 | success |
| YOLO检测 | 104 | 🤖 | warning |

## 💡 推荐方案

### 方案1：保持 Emoji，修改背景色为淡粉色
- ✅ 简单快速
- ✅ 无需准备资源
- ✅ 兼容性好

### 方案2：使用图片图标
- ✅ 更专业
- ✅ 可自定义设计
- ⚠️ 需要准备图标文件

### 方案3：使用图标字体库
- ✅ 图标丰富
- ✅ 可缩放不失真
- ⚠️ 需要引入字体库

## 🔧 快速修改步骤

1. **打开文件**：`pages/index/index.vue`
2. **找到图标位置**：搜索 `class="icon"`
3. **替换 emoji**：修改 `<text class="icon">` 中的内容
4. **修改背景色**（可选）：修改 `.icon-wrapper` 的背景色样式
5. **保存并测试**

## 📚 相关文件

- **首页文件**：`pages/index/index.vue`
- **静态资源**：`static/` 目录
- **全局样式**：`App.vue`

